import { AddCategoryRequest } from './add-category-request';

describe('AddCategoryRequest', () => {
  it('should create an instance', () => {
    expect(new AddCategoryRequest()).toBeTruthy();
  });
});
